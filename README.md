# 📚 Библиотека склонения слов

Легкая и расширяемая JavaScript-библиотека для склонения слов на русском и казахском языках. Работает без зависимостей, подключается через `<script>` тег.

## ✨ Особенности

- 🌍 **Два языка**: русский и казахский
- 🎯 **Простой API**: всего несколько функций для всех задач
- 📦 **Без зависимостей**: чистый Vanilla JavaScript
- 🔧 **Расширяемость**: легко добавить новые языки
- 🎨 **Гибкость**: работает с DOM-элементами и строками
- ⚡ **Легковесность**: минимальный размер библиотеки

## 📥 Установка

Вариант 1: Использование объединенной версии (рекомендуется):

```html
<!-- Объединенная версия - включает все модули -->
<script src="declension-all.js"></script>
```

Вариант 2: Подключение модулей отдельно:

```html
<!-- Основная библиотека -->
<script src="declension.js"></script>

<!-- Модуль русского языка -->
<script src="lang-ru.js"></script>

<!-- Модуль казахского языка -->
<script src="lang-kz.js"></script>
```

## 🚀 Быстрый старт

### Склонение одного слова

```javascript
// Русский язык
Declension.inflectWord('книга', 'ru', 'genitive');
// → "книги"

Declension.inflectWord('стол', 'ru', 'dative');
// → "столу"

// Казахский язык
Declension.inflectWord('кітап', 'kz', 'ilik');
// → "кітаптың"

Declension.inflectWord('үй', 'kz', 'barys');
// → "үйге"
```

### Склонение текста из DOM-элемента

```html
<input id="name" value="Александр">
<div id="result"></div>

<script>
Declension.inflect({
  selector: '#name',
  lang: 'ru',
  case: 'genitive',
  target: '#result'
});
// В #result появится: "Александра"
</script>
```

## 📖 Документация

### API методы

#### `inflectWord(word, lang, caseType)`

Склоняет одно слово в указанный падеж.

**Параметры:**
- `word` (string) - слово для склонения
- `lang` (string) - код языка: `'ru'` или `'kz'`
- `caseType` (string) - тип падежа (см. ниже)

**Возвращает:** `string` - склоненное слово

**Пример:**
```javascript
Declension.inflectWord('документ', 'ru', 'genitive');
// → "документа"
```

---

#### `inflect(options)`

Склоняет текст из DOM-элемента и выводит результат.

**Параметры:**
- `options.selector` (string) - CSS-селектор элемента с текстом
- `options.lang` (string) - код языка: `'ru'` или `'kz'`
- `options.case` (string) - тип падежа
- `options.target` (string, опционально) - CSS-селектор для вывода результата

**Возвращает:** `string` - склоненный текст

**Пример:**
```javascript
Declension.inflect({
  selector: '#input-field',
  lang: 'kz',
  case: 'ilik',
  target: '#output-field'
});
```

---

#### `inflectText(text, lang, caseType, firstWordOnly)`

Склоняет текст (одно или несколько слов).

**Параметры:**
- `text` (string) - текст для склонения
- `lang` (string) - код языка
- `caseType` (string) - тип падежа
- `firstWordOnly` (boolean, по умолчанию `true`) - склонять только первое слово

**Пример:**
```javascript
Declension.inflectText('красная книга', 'ru', 'genitive', true);
// → "красной книга"
```

---

### Падежи

#### Русский язык (`lang: 'ru'`)

| Падеж | Код | Вопрос | Пример |
|-------|-----|--------|--------|
| Именительный | `nominative` | кто? что? | книга |
| Родительный | `genitive` | кого? чего? | книги |
| Дательный | `dative` | кому? чему? | книге |
| Винительный | `accusative` | кого? что? | книгу |
| Творительный | `instrumental` | кем? чем? | книгой |
| Предложный | `prepositional` | о ком? о чём? | книге |

#### Казахский язык (`lang: 'kz'`)

| Септік | Код | Вопрос | Пример |
|--------|-----|--------|--------|
| Атау | `ataw` | кім? не? | кітап |
| Ілік | `ilik` | кімнің? ненің? | кітаптың |
| Барыс | `barys` | кімге? неге? | кітапқа |
| Табыс | `tabys` | кімді? нені? | кітапты |
| Жатыс | `jatys` | кімде? неде? | кітапта |
| Шығыс | `shygys` | кімнен? неден? | кітаптан |
| Көмектес | `komektes` | кіммен? немен? | кітаппен |

---

## 💡 Примеры использования

### Пример 1: Склонение русских имен

```javascript
const names = ['Александр', 'Мария', 'Иван'];

names.forEach(name => {
  const genitive = Declension.inflectWord(name, 'ru', 'genitive');
  console.log(`Нет ${genitive}`);
});

// Вывод:
// Нет Александра
// Нет Марии
// Нет Ивана
```

### Пример 1.5: Склонение ФИО с несклоняемыми фамилиями

```javascript
// Несклоняемые казахские фамилии
Declension.inflectWord('Жайлау Ерасыл', 'ru', 'genitive', { type: 'name' });
// → "Жайлау Ерасыла" (фамилия не склоняется!)

Declension.inflectWord('Хан Андрей', 'ru', 'dative', { type: 'name' });
// → "Хан Андрею" (фамилия не склоняется!)

// Корейские фамилии
Declension.inflectWord('Анна Ким', 'ru', 'genitive', { type: 'name' });
// → "Анны Ким" (фамилия не склоняется!)

Declension.inflectWord('Иван Пак', 'ru', 'instrumental', { type: 'name' });
// → "Иваном Пак" (фамилия не склоняется!)

// Двойные фамилии
Declension.inflectWord('Анна Смирнова-Ким', 'ru', 'genitive', { type: 'name' });
// → "Анны Смирновой-Ким" (Смирнова склоняется, Ким - нет!)

// Обычные русские фамилии
Declension.inflectWord('Иванов Иван', 'ru', 'genitive', { type: 'name' });
// → "Иванова Ивана" (всё склоняется)
```

### Пример 2: Склонение казахских слов

```javascript
const words = ['қала', 'үй', 'мектеп', 'кітап'];

words.forEach(word => {
  console.log(`${word} → ${Declension.inflectWord(word, 'kz', 'ilik')}`);
});

// Вывод:
// қала → қаланың
// үй → үйдің
// мектеп → мектептің
// кітап → кітаптың
```

## 🎯 Особенности реализации

### Русский язык

- Поддержка трех родов: мужской, женский, средний
- Различие твердой и мягкой основы
- Словарь исключений для особых случаев (путь, мать, дочь)
- Автоматическое определение типа слова
- **Поддержка несклоняемых фамилий**: автоматическое определение и правильная обработка казахских и других несклоняемых фамилий (Жайлау, Хан, Бек, Нур и т.д.)

### Казахский язык

- Гармония гласных (передний/задний ряд)
- Правильный подбор аффиксов в зависимости от окончания слова
- Поддержка звонких и глухих согласных
- Различие носовых согласных (н, ң, м) и других звонких
- Поддержка русских букв в казахских именах

---

## 🔧 Расширение библиотеки

Вы можете легко добавить поддержку нового языка:

```javascript
// Создайте модуль языка
const MyLanguageModule = {
  inflect: function(word, caseType) {
    // Ваша логика склонения
    return word + 'suffix';
  }
};

// Зарегистрируйте модуль
Declension.registerLanguage('my', MyLanguageModule);

// Используйте
Declension.inflectWord('word', 'my', 'case');
```

---

## 📝 Примечания

### Ограничения

- Библиотека работает только с единственным числом
- Склонение основано на упрощенных правилах
- Некоторые исключения могут не поддерживаться
- Для сложных случаев рекомендуется использовать словарь исключений

### Рекомендации

- Для имен собственных лучше всего работает библиотека
- Для обычных слов результат может требовать проверки
- Используйте словарь исключений для особых случаев
- Тестируйте результаты на реальных данных

### Добавление несклоняемых фамилий

Если вам нужно добавить новую несклоняемую фамилию, отредактируйте функцию `isUndeclinableSurname` в файле `declension-all.js`:

```javascript
function isUndeclinableSurname(surname) {
  const lowerSurname = surname.toLowerCase();

  // Добавьте вашу фамилию в этот список
  const kazakhPatterns = [
    /^(жайлау|хан|бек|би|тау|кул|кан|бай|жан|нур|ас|ер|кош|тас|жас|сай|су|кор|дос)$/i
  ];

  // ... остальной код
}
```

---

## 🤝 Вклад в проект

Если вы нашли ошибку или хотите улучшить библиотеку:

1. Добавьте слова в словарь исключений
2. Улучшите правила склонения
3. Добавьте новые языки
4. Создайте pull request

---

## 📄 Лицензия

MIT License - используйте свободно в своих проектах!

---

## 👥 Автор

  Zhailau Yerassyl

---

## 🔗 Полезные ссылки

- [Тест русского языка](test-ru.html)
- [Тест казахского языка](test-ru.html)

---

**Приятного использования! 🎉**
